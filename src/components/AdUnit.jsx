// src/components/AdUnit.jsx
// Built now — activate after AdSense approval by uncommenting the script

export default function AdUnit({ slot }) {
  return (
    <div style={{ textAlign:"center", margin:"2rem 0" }}>
      {/* Uncomment below after AdSense approval
      <ins className="adsbygoogle"
        style={{ display:"block" }}
        data-ad-client="ca-pub-XXXXXXXXXXXXXXXX"
        data-ad-slot={slot}
        data-ad-format="auto"
        data-full-width-responsive="true" />
      */}
    </div>
  )
}